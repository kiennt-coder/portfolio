export enum THEME {
    LIGHT = 'light',
    DARK = 'dark',
    SYSTEM = 'system'
}

export enum LOCAL_STORAGE_KEY {
    THEME = 'theme',
}

export enum DARK_MODE {
    CLASS = 'class',
    DATA_THEME = 'data-theme'
}

export enum TRIGGER {
    CLICK = 'click',
    HOVER = 'hover',
}

export enum PLACEMENT {
    TOP = 'top',
    BOTTOM = 'bottom',
    LEFT = 'left',
    RIGHT = 'right',
}